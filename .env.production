# PostgreSQL Database (hosted)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=fAdvot-vyggeg-1rysku
POSTGRES_SERVER=b.htvauholjqlrfihihszd.supabase.co
POSTGRES_PORT=5432
POSTGRES_DB=postgres

# Application settings
DOMAIN=banwee.com  # Replace with actual domain (e.g., api.example.com)
ENVIRONMENT=production

# CORS allowed origins (add frontend/proxy domains)
BACKEND_CORS_ORIGINS=http://banwee.com,https://banwee.com,https://www.banwee.com

# Redis (for cache, celery broker, result backend)
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/1
CELERY_RESULT_BACKEND=redis://redis:6379/2

# Security
SECRET_KEY=change_this_to_a_long_random_string_for_prod
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Optional external service keys (e.g., SMS, Firebase)
SMS_API_KEY=your-sms-api-key
SMS_API_URL=https://api.yoursmsprovider.com/send
FIREBASE_CREDENTIALS_JSON=/usr/src/app/secrets/firebase.json

# SQLite fallback (rarely needed in prod)
SQLITE_DB_PATH=/tmp/db1.db

SMTP_HOSTNAME=smtp.gmail.com
SMTP_USER=youremail@gmail.com
SMTP_PASSWORD=your_app_password_or_actual_password

GOOGLE_SERVICE_ACCOUNT_JSON='{
  "type": "service_account",
  "project_id": "...",
  "private_key_id": "...",
  "private_key": "-----BEGIN PRIVATE KEY-----\nMIIEvgIBADANBgkqhkiG9w0BA...rest_of_key\n-----END PRIVATE KEY-----\n",
  "client_email": "...",
  "client_id": "...",
  ...
}'

TELEGRAM_BOT_TOKEN=